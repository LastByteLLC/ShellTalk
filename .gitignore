.DS_Store
.vscode/
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
*.wasm

# Meta-harness run artifacts (overlays, per-candidate traces, metrics, notes).
# Local-only — only condensed findings at harness/*.md are tracked.
harness/runs/
*.lock

# Discovery layer: uncompressed tldr corpus is regeneratable from the
# committed .json.gz via gunzip and from upstream via refresh-tldr-baseline.sh.
# Only the gzipped corpus + metadata sidecar are tracked.
Sources/ShellTalkDiscovery/Resources/tldr-baseline.json
